#include <bits/stdc++.h>

using namespace std;

void sol()
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int tests;
    cin >> tests;

    while (tests-- > 0)
    {
        ios::sync_with_stdio(false);
        cin.tie(nullptr);
        void();
    }
}